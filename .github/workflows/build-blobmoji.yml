name: blobmoji

on:
  schedule:
    - cron: "8 5 * * 1"        # Mondays — rebuilds only if blobmoji upstream changed
  workflow_dispatch:
    inputs:
      force:
        description: "Build even if upstream is unchanged"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      set: blobmoji
      force: ${{ github.event.inputs.force == 'true' }}
    secrets: inherit
