name: noto

on:
  schedule:
    - cron: "0 5 * * 1"        # Mondays — rebuilds only if noto's upstream changed
  workflow_dispatch:
    inputs:
      force:
        description: "Build even if upstream is unchanged"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      action: noto
      force: ${{ github.event.inputs.force == 'true' }}
    secrets: inherit
