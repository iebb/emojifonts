name: tossface

on:
  schedule:
    - cron: "0 6 * * 1"        # Mondays — rebuilds only if tossface upstream changed
  workflow_dispatch:
    inputs:
      force:
        description: "Build even if upstream is unchanged"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      set: tossface
      force: ${{ github.event.inputs.force == 'true' }}
    secrets: inherit
