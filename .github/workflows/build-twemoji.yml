name: twemoji

on:
  schedule:
    - cron: "24 5 * * 1"        # Mondays — rebuilds only if twemoji upstream changed
  workflow_dispatch:
    inputs:
      force:
        description: "Build even if upstream is unchanged"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      set: twemoji
      force: ${{ github.event.inputs.force == 'true' }}
    secrets: inherit
